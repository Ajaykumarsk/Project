export interface CanteenFoodMenu{
    id: number; // Optional field for when the item is being created
    name: string;
    menu: string;
    quantity: string;
    calorie: number;
    item_photo: string; // Optional field for when the item photo is not provided

}